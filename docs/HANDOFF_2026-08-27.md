# GKOS-Engine build handoff

Handoff date: 2026-08-27

## Accepted repository coordinate

| Item | Coordinate or disposition |
| --- | --- |
| Repository | `Odenknight/GKOS-Engine` |
| Accepted branch | `main` |
| Accepted merge commit | `77b5cacd0e01da99fbb4237eac8358f6303e0e74` |
| Delivery PR | #34, merged 2026-08-27 10:58:32 UTC |
| Post-merge CI | Run `33065334394`, success |
| Package version | `2.1.2` |
| Release standing | The `v2.1.2` tag points to predecessor `7bf14b481e78c5ae9d1e14661602be4f24559d0e`; current `main` additionally contains the successor-F1 correction merged by PR #34. No new tag or release was created for that correction. |

## Work completed

- Integrated and qualified the successor-F1 Draft.1 identity/MCP contract pack
  on the then-current `main` without changing Draft.2, runtime, admission-policy,
  or Engine-Lite product state.
- Added the separately named Draft.1 generator and corrected all eleven Draft.1
  workflow invocations.
- Preserved accepted historical source coordinates while adapting live paths to
  the Draft.1 generator name.
- Added and qualified the Q24 bootstrap handoff/replay contract behavior.
- Corrected the Windows-CRLF/Linux-LF generator-manifest digest so the pack is
  reproducible from committed bytes on both platforms.
- Rebased onto the concurrent `v2.1.2` documentation update and advanced the
  protected-diff evidence base before delivery.

## Verification summary

- Draft.1 contract suite: 35/35 passed.
- Draft.2 conformance suite: 6/6 passed.
- Generator check and exact manifest closure: passed.
- TypeScript typecheck and production build: passed.
- Protected-path and allowed-diff gates: passed.
- Node 22, 23, and 24 build matrices: passed.
- Linux and Windows watcher matrices: passed.
- Windows retrieval-path-security matrices: passed.
- Post-merge `main` CI: passed.
- Two independent Sol integration reviews: accepted.

One Windows Node 24 watcher measurement exceeded its hard latency threshold
while duplicate matrices were running. The unchanged job passed on its targeted
rerun and on the authoritative PR merge-ref run. No threshold, retry logic, or
watcher code was weakened.

## Product standing

See [CURRENT_CAPABILITIES.md](CURRENT_CAPABILITIES.md) for the complete current
matrix. Important boundaries for the next builder are:

- Navigation remains source-content read-only; Navigation Effects is absent.
- Proposal ingress remains disabled.
- Identity/MCP Draft.2 is integration-qualified for seven read-only tools, not
  production-conformant across the full contract.
- Admission-policy receipts are evidence, not approval or activation authority.
- Watcher, index, status, token, and export writes are derived-state or explicit
  output effects, not silent source-note mutation.

## Preserved parallel work

- The stopped dirty F2 worktree was not modified or cleaned.
- Engine-Lite admission and its active functional-uplift work were not modified
  by the successor-F1 merge.
- No release, deployment, activation, or publication action was performed by
  the successor-F1 workstream.

## Recommended next decisions

1. Decide whether current `main` should receive a new release coordinate,
   because it contains PR #34 after the existing `v2.1.2` tag.
2. Resume F2 only from its preserved worktree and re-audit it against current
   `main`; do not mix its dirty state into a documentation or release branch.
3. Treat Navigation Effects, proposal ingress, production MCP conformance, and
   release/deployment work as separate major build decisions.
4. Re-pin downstream distributions, including Engine-Lite, only after their
   compatibility and platform matrices pass against the selected Full commit.

