import {execFileSync} from 'node:child_process';import {mkdirSync,writeFileSync}from'node:fs';import{createHash}from'node:crypto';import{join}from'node:path';
const root='[LOCAL_PATH]';const standard='[LOCAL_PATH]';const pin='8f2a158c6d4b8cabd907d98765766d281aec1247';
const dest=join(root,'contracts/documentation-verifier/v0.81');mkdirSync(dest,{recursive:true});const hashes={};for(const name of['refusal-receipt.schema.json','gkx-common.defs.json']){const bytes=execFileSync('git',['-C',standard,'show',pin+':schemas/'+name]);const hash=createHash('sha256').update(bytes).digest('hex');if(name==='refusal-receipt.schema.json'&&hash!=='11017113221f8ebc51b03c7de8078bdba3258a7d32b084d675ffd3de28c0182f')throw Error('SCHEMA_PIN_MISMATCH');writeFileSync(join(dest,name),bytes);hashes[name]=hash;}writeFileSync(join(dest,'provenance.json'),JSON.stringify({repository:'https://github.com/Odenknight/gkos-standard',tag:'v0.81',commit:pin,schemaVersion:'1.0.0',files:hashes},null,2)+'\n');console.log(JSON.stringify(hashes));