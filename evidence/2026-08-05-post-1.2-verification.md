# GKOS-Engine post-1.2 verification evidence

## Scope and immutable source binding

This record documents verification of the exact GKOS-Engine implementation source at:

- Implementation commit: `187396ea22df6b3a34749dddbcf3b4b866a147d7`
- Implementation tree: `d5da2b732070853180ed6f6f613a7e450b6497ec`
- Commit subject: `Align post-1.2 Engine with accepted GKOS requirements`

The evidence record was authored and committed only after the verification run. Its
documentation-only commit is therefore not the tested source and is intentionally
not used as a recursive verification binding. No implementation files changed after
the commands below completed and before the implementation commit was created.

## Standard authority

The requirement identifiers and adapter coverage used by the implementation are
anchored to the companion GKOS Standard repository commit:

- Standard commit: `8d96108b5e7255c2702230a75f3296e64ca0923a`
- Registry: `gkos-standard/requirements/REGISTRY.md`
- Adapter map: `gkos-standard/conformance/adapters/gkos-engine.requirements.json`

The implementation's requirement mapping is documentary and non-normative. The
starter graph expectation `GCP3-L01` covers `GKOS-LINEAGE-001` and
`GKOS-LINEAGE-003`; it does **not** cover `GKOS-LINEAGE-002`, and its catalog status
remains `UNEVALUATED`.

## Verification run

- Started: `2026-08-05T06:50:36.449Z` (UTC)
- Operating system: `Microsoft Windows NT 10.0.26200.0`
- Shell: `PowerShell 7.6.3`
- Node.js: `v24.18.0`
- npm: `11.16.0`

Commands were run from the GKOS-Engine repository root against the implementation
tree identified above.

### Type checking

Command:

```text
npm run typecheck
```

Raw result summary:

```text
exit code: 0
```

### Test suite

Command:

```text
npm test
```

Raw Node test-runner summary:

```text
tests 180
suites 0
pass 180
fail 0
cancelled 0
skipped 0
todo 0
duration_ms 760.3119
exit code: 0
```

### License metadata

Command:

```text
npm run check:license
```

Raw result summary:

```text
license metadata consistent: Apache-2.0
exit code: 0
```

### Package contents

Command:

```text
npm run pack:check
```

Raw result summary:

```text
package contents verified: 70 files, 244056 bytes
exit code: 0
```

### Patch hygiene

Command:

```text
git diff --check
```

Raw result summary:

```text
exit code: 0
```

Git also emitted informational Windows line-ending warnings about LF being replaced
by CRLF. It reported no whitespace errors.

## Release and conformance status

This evidence does not make a release claim or a GKOS conformance claim. The work is
post-1.2 and unreleased; `package.json`, the package-lock root, and the runtime version
constant remain `1.2.0`. The companion GKOS Standard target is likewise unpublished
v0.77. Open graph-semantics topics and catalog evaluation remain governed by the
standard process rather than being resolved by this implementation record.
