# Engine 2.2.0 managed-MOC build evidence

Development candidate on `feature/engine-2.2-managed-mocs`, based on TS main
`d81f9d1351f1a9228650a840629191a92f2dfb22`. Plans/report published at
`a2777d92d0ad9cc56bfb52a413f60c5d17607002`; Rust documentation published at
`20a85e940d1975f6904a1abe0e1ed7ebe856d2f6` on
`Odenknight/GKOS-Engine-Rust@docs/moc-build-plan-20260906`.

## Implemented

- 2.2.0 package/runtime/optional Python sidecar identity; contract versions and
  frozen historical/oracle evidence remain unchanged.
- Deterministic managed-MOC batch planning, explicit tags/links, MOC grouping,
  optional bounded proposal-only LLM interface and offline fallback.
- Durable coalesced admission, affected target scopes, startup/periodic/manual
  reconciliation, recursive watching with passive fallback and self-event checks.
- Checksummed host state, mandatory live preconditions, guarded source snapshots,
  journal/archive/CAS execution, committed ownership advancement and recovery.
- Exact human-region preservation, empty-scope updates, ambiguity refusal,
  source-markup checks and a linear discovery-membership lookup.
- Runnable synthetic-vault example and planning smoke benchmark.

## Local checks to date

Windows, Node 24.18.0. Typecheck and build pass. Navigation/Effects passed
150/150 before additional runtime/region/empty-scope tests. Updated assistance/
runtime tests passed 20/20; final host tests passed 7/7. The example created a
MOC and shut down cleanly in a new synthetic temporary vault.

License and nomenclature pass. Package check passed at 587 files/6,670,180 bytes
before subsequent example/docs updates; final packing must be rechecked.
Python sidecar tests pass 6/6. The existing development-only fast-uri dependency
was updated; npm reports zero vulnerabilities. No production dependency added.

Planning smoke samples for one target directory were 10.616 ms / 100 notes,
52.624 ms / 2,000, 224.971 ms / 10,000 and 988.978 ms / 50,000. Proposed digests
matched across tiers. Last-tier sampled RSS was 396,410,880 bytes. These are
single samples under concurrent suite load, not P95 convergence qualification.

## Remaining qualification and scope

Full current tests and exact-head hosted checks are in progress at this report
stage. No completed cross-platform qualification, 24-hour soak, physical
power-loss test, live-model quality evaluation, signing or release is claimed.
Kosmos adapter/UI and final released artifact adoption remain downstream work.
The host requires a validated snapshot/index and current authority provider;
the synthetic example is not a production authority registry.

The Node adapter retains its cooperative-vault threat model and does not claim
directory-entry durability across power loss. Unsupported or ambiguous recovery
blocks writes. Remaining limits are release gates, not acceptance waivers.

## Upgrade diagnostics and repairs

The initial diagnostic full run reported 1,062 tests / 1,024 passes / 38 failures.
It overlapped source/build changes and is not exact-head qualification. Repairs
preserve frozen fixture bytes: canonical LF checkout, explicit additive 2.2
export/help/provenance expectations, and qualified 2.1.2 retrieval producer
validation. Pure historical fixture derivation uses the record's producer
identity; new physical generations still emit 2.2.0. Unknown producers fail
closed. An authority-negative test now checks the exact physical journal error,
not whether a redacted message happens to contain a code prefix.

The corrected compatibility baseline passes 4/4; the three previously failing
evaluation fixture/reviewed-bundle cases pass with historical bytes unchanged.
A concurrent diagnostic also saw missing dist modules while another build
replaced that directory. Final qualification must run against a stable build
without concurrent source edits or rebuilds.

At `2196d128344f68bde928ad772ff59f40726e2c04`, hosted Linux Node 22 full tests
reported 1,076 / 1,068 pass / 7 fail / 1 skip. They exposed historical evaluation
database replay and current-producer fixture expectation gaps plus the incorrect
authority-error assertion. The local same-commit run reported 971 / 960 / 11:
an overlapping package dry-run unexpectedly invoked `prepare` despite its
ignore-scripts environment, temporarily removing build modules and invalidating
the intended isolated run. It is retained as diagnostic evidence, not a pass.

The next repair adds validated immutable, unactivated historical evaluation
restoration, explicit 2.2 physical result coordinates, and detached, complete
coordinator limits. Focused upgrade/assistance tests pass 20/20. Exact-head full
qualification remains outstanding until the next isolated run completes.

The repaired complete retrieval evaluation CLI and provenance suites pass 41/41
on Windows Node 24.18.0, including the exhaustive tuning grid and byte-exact
historical replay. New physical generation and search-result digests are locked
separately for lexical scan and FTS5; frozen source fixtures remain untouched.

Final repair preflight: typecheck passes, Navigation/Effects passes 156/156,
and package contents verify at 588 files / 6,665,174 bytes. Packaging completed
before the next full test run; no concurrent package command is permitted during
that run. This paragraph records preflight, not full-suite qualification.
