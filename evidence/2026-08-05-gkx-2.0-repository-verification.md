# GKX 2.0 repository-surface verification

**Date:** 2026-08-05

**Scope:** GKOS-Engine 2.0.0 source, tests, generated artifacts, documentation,
release metadata, workflows, services, examples, CLI, and package surface.

## Contract verified

- Product identity remains **GKOS-Engine**.
- Schema, command, field, state-path, diagnostic, and public API namespaces are
  **GKX**.
- The public command is `gkx` and examples invoke `node bin/gkx.mjs`.
- Documents use `gkx_version`, `.gkx/`, `GKX-*`, and `Gkx*` public API names.
- Current release documentation and traceability identify version `2.0.0`.
- The canonical package artifact is `dist/gkos-engine.mjs`.
- Removed naming aliases are absent from repository-surface documentation and
  operational files.

## Verified results

- TypeScript typecheck: PASS.
- Clean build: PASS; the build deletes and recreates `dist/` to prevent stale
  pre-2.0 artifacts from entering a package.
- Test suite: 181/181 PASS.
- License metadata: PASS, Apache-2.0.
- Package check: PASS, 69 files and 240,557 bytes.
- `gkx --help`: PASS; no positional compatibility command is accepted.
- Case-insensitive filesystem content scan for the retired namespace: zero.
- Filesystem path scan for retired source, CLI, declaration, and artifact names:
  zero.
- Compatibility-shim and false external-schema-label scan: zero.
- `git diff --check`: PASS; informational Windows line-ending warnings only.

## Boundaries

These results bind to the current uncommitted
`agent/gkx-2.0-breaking-rebuild` working tree. A release evidence record must
repeat the checks against an exact signed commit. This evidence does not claim
standard conformance, independent certification, or release publication.
